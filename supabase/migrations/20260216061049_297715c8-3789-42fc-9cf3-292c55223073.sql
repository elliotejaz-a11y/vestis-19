-- Update the email templates to use "Vestis" branding
-- This changes the confirmation email subject/content
-- Note: The actual sender name is configured via Supabase auth config, 
-- but we can update the site_url name reference in the auth schema
-- The email template changes need to be done via config

SELECT 1; -- placeholder, email config done via configure-auth and config.toml
